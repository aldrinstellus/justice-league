# Penpot API Configuration
# Copy this file to .env and fill in your credentials

# Penpot API URL (use your self-hosted instance or official cloud)
PENPOT_API_URL=https://design.penpot.app

# Penpot Credentials
PENPOT_USERNAME=your_email@example.com
PENPOT_PASSWORD=your_password_here

# Optional: Specific team or project IDs
# PENPOT_TEAM_ID=team-uuid-here
# PENPOT_PROJECT_ID=project-uuid-here

# ===========================================
# Figma API Configuration
# ===========================================
# Get your token from: Figma → Settings → Account → Personal Access Tokens
# Required for all Figma operations (frame export, component conversion, etc.)
FIGMA_ACCESS_TOKEN=figd_your_token_here

# ===========================================
# Quicksilver Speed Export Configuration
# ===========================================
# Production-tested settings for reliable Figma frame export
# These defaults work for 99% of Figma files (tested with 484 frame files)
#
# Concurrent workers for parallel export (default: 8)
# QUICKSILVER_MAX_WORKERS=8
#
# Frames per API batch request (default: 15)
# QUICKSILVER_BATCH_SIZE=15
#
# API request timeout in seconds (default: 60)
# Increase if you get timeout errors on large/complex files
# QUICKSILVER_API_TIMEOUT=60
#
# CDN download timeout in seconds (default: 120)
# Increase if downloading very large PNG files
# QUICKSILVER_CDN_TIMEOUT=120
#
# Maximum retry attempts per failed frame (default: 5)
# QUICKSILVER_MAX_RETRIES=5

# ===========================================
# Mission Control Narrator (v2.0)
# ===========================================
# Control narrative verbosity for Justice League operations
# Options:
#   - narrative: Full banter + sequential thinking + team coordination (DEFAULT)
#   - technical: Legacy logger.info() style (for developers)
#   - silent: Minimal output only (for CI/CD)
#   - debug: Full logs + banter (for troubleshooting)
NARRATOR_MODE=narrative
